name: "✨ On push to main branch"
on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  publish-docs:
    name: '️‍📚️ Publish Docs'
    uses: ./.github/workflows/docs.yaml